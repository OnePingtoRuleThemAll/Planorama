<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Planorama</title>
</head>

<body>
  <div class="wrapper">
    <header class="hero">
      <div>
        <nav class="nav">
          <a class="link" href="/">Home</a>
          <a class="link" href="/dashboard">Dashboard</a>
          {{#if loggedIn}}
          <a class="link" href="./js/logout.js" id="logout">Logout</a>
          <a class="link" href="./profile.handlebars">Profile</a>
          <a class="link" href="./createEvent.handlebars">Create New Event</a>
          {{else}}
          <a class="link" href="./js/login.js">Login</a>
          {{/if}}
        </nav>
      </div>

      <h1 class="app-title"><i class="fab"></i>Planorama</h1>
    </header>

    <main>
      {{{ body }}}
    </main>
  </div>

  {{#if loggedIn}}
  <script src="./js/logout.js"></script>
  {{/if}}

</body>

</html>